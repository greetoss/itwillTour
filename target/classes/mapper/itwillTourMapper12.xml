<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">


<mapper namespace="com.exe.itwillTourDAO.ItwillTourDAO12">
	
	<select id="getNoticeInfo" resultType="map">
		select * from ${notice} order by noticeno desc
	</select>
	
	
	
	
		<select id="getSearchSubInfo" resultType="map">
		select * from notice where noticesubject like '%'||#{search_keyword}||'%' order by noticeno desc
	</select>
	
			<select id="getSearchConInfo" resultType="map">
		select * from notice where noticecontent like '%'||#{search_keyword}||'%' order by noticeno desc
	</select>
	
				<select id="getSearchSub_ConInfo" resultType="map">
		select * from notice where noticesubject like '%'||#{search_keyword}||'%' or 
		noticecontent like '%'||#{search_keyword}||'%' order by noticeno desc
	</select>


</mapper>