<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.exe.itwillTourDAO.ItwillTourDAO11">
	
	<select id="getUserInfo" resultType="map">
		select * from member where userid = #{id}
	</select>
	
	<insert id="submitInquiry" parameterType="map">
		insert into QnA2 
		(qna2no, qna2booking, qna2created, qna2category, qna2username, qna2subject, qna2content, ${condition1} ${fileColumn})
		values(qna2_inc.nextval, '답변대기중', SYSDATE, #{category}, #{user_name}, #{subject}, #{content}, #{condition2} ${fileValue})
	</insert>

</mapper>