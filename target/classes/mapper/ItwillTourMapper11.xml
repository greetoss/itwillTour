<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">


<mapper namespace="com.exe.itwillTourDAO.ItwillTourDAO11">

	<insert id="submitInquiry" parameterType="map">
		insert into QnA2 
		(QNA2CATEGORY, QNA2USERNAME, QNA2SUBJECT, QNA2CONTENT, QNA2FILE1, QNA2FILE2, QNA2FILE3, ${condition1})
		values(#{category}, #{user_name}, #{subject}, #{content}, #{file1}, #{file2}, #{file3}, #{condition2} )
	</insert>
	
	<select id="getUserInfo" resultType="map">
		select * from member where userid = #{id}
	</select>

</mapper>